'use strict';

const {app, mock, assert} = require('egg-mock/bootstrap');
const {loginAction} = require('../utils/apis');

describe('test/app/controller/admin.test.js', () => {
  beforeEach(async() => {
  });
});
